# PostgreSQL (Prisma) — sessions, messages, negotiations, cache, memory
# Start Postgres: cd agent-service && docker compose up -d
# When Postgres runs via docker-compose: postgresql://postgres:postgres@localhost:5436/agentdb
# When app runs inside Docker too: use host "postgres" and port 5432
# If you get P1000 "credentials not valid", recreate DB: docker compose down -v && docker compose up -d then npx prisma migrate deploy
DATABASE_URL=postgresql://postgres:postgres@localhost:5436/agentdb

# MySQL (Sequelize) — for storing job match quotes (negotiation response per job+provider)
DB_DATABASE=agent_db
DB_USERNAME=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306

# Laravel API (customer/on-demand endpoints)
LARAVEL_API_BASE_URL=http://localhost:8000/api

# OpenAI (for LangChain/LangGraph LLM)
OPENAI_API_KEY=

# LangSmith (tracing: agent, human messages, tool calls)
# Required for runs to show data at https://smith.langchain.com
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=
# Optional: LANGCHAIN_PROJECT=fixerity-agent

# Mem0 Platform (memory for buyer/seller agents)
MEM0_API_KEY=

# Redis (conversation history for buyer/seller chat; per-job/per-order)
REDIS_URL=redis://localhost:6379

# Agent service port (Express buyer/seller agent API)
PORT=3001

# Frontend (Next.js): copy to frontend/.env.local
# NEXT_PUBLIC_AGENT_API_URL=http://localhost:3001
# NEXT_PUBLIC_API_URL=http://localhost:8000
# or: AGENT_SERVICE_PORT=3001

# Optional: shared secret for POST /webhook/provider-registered (X-Webhook-Secret header)
# WEBHOOK_SECRET=your-secret

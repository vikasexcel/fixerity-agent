services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: fixerity-agent-db-vector
    restart: unless-stopped

    environment:
      POSTGRES_USER: fixerity
      POSTGRES_PASSWORD: fixerity123
      POSTGRES_DB: fixerity_agent_db_vector

    ports:
      - "5437:5432"

    volumes:
      - fixerity-agent-db-vector-data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fixerity"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  fixerity-agent-db-vector-data:
